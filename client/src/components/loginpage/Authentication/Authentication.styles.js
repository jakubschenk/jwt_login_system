import styled from 'styled-components';
import { Text } from '../Boxes/Boxes.styles';


export const ErrorText = styled(Text)`
  color: var(--error);
`

export const SuccesText = styled(Text)`
  color: var(--succes);
`
